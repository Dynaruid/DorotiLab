# Doroti templates

Install this package and run `dotnet new doroti-counter` to create a Windows desktop app using the source-ported `Doroti.Host.Desktop` shell and its strict asynchronous A2 GPU pipeline. The generated app does not select a rendering backend or software fallback directly.
