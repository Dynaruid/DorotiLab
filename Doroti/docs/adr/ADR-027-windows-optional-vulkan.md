# ADR-027: Experimental optional Silk.NET Vulkan presenter

- Status: Experimental
- Date: 2026-09-02
- Last updated: 2026-09-04

## Decision

The Windows App SDK `HwndExactCpp` target keeps managed hardware-D3D11 ANGLE/EGL as its default presenter. `DOROTI_WINDOWS_PRESENTER=Vulkan` explicitly selects a separate direct Vulkan/Skia presenter; there is no automatic Vulkan-to-ANGLE fallback. On Windows 11 24H2+, an explicit Vulkan selection may be combined with `WindowBackdropMode.experimentalAcrylic`. That combination preserves the Vulkan presenter and fails explicitly if its Desktop Acrylic target cannot attach; it never substitutes the ANGLE ContentIsland presenter.

The product resolves only the System32 `vulkan-1.dll`, rejects software devices, and requires Vulkan 1.1, a graphics queue, a valid Windows adapter LUID, and importable dedicated D3D11-texture external memory (`VK_KHR_external_memory`, `VK_KHR_external_memory_win32`, `VK_KHR_get_memory_requirements2`, and `VK_KHR_dedicated_allocation`). The current product presenter owns zero Vulkan WSI swapchains; `VK_KHR_surface`, `VK_KHR_win32_surface`, and `VK_KHR_swapchain` are exercised only by separate legacy WSI evidence. If exactly one device satisfies the complete product contract it is selected; multiple capable devices require `DOROTI_WINDOWS_VULKAN_DEVICE` to be an exact or unique device-name fragment.

## Ownership and synchronization

Skia paints a capacity-size wrapper over a grow-only, bounded device-local Vulkan backing image. The complete retained capacity is initialized with application-owned pixels while scene drawing is clipped to the current logical viewport, so newly exposed pixels do not require scene scaling. The presenter copies from that backing into one of three exact-LUID D3D11 shared textures imported as dedicated Vulkan external memory. Copy-fence completion is confirmed before the native Windows Presentation API commit, and each Presentation buffer's availability event is the reuse and teardown authority. If a grown allocation exceeds the retention bound or returns host/device OOM, it is discarded before submission and the exact requested extent is tried once.

The product presenter rejects stale work before selecting an available Presentation buffer. Once selected, it completes the Vulkan copy and commits that same slot to Windows Presentation; there is no visible-HWND WSI acquire/present transaction. A negative or indeterminate Presentation result poisons the manager rather than pretending that buffer ownership is known. Device loss abandons the Skia context before renderer wrappers and native Vulkan handles are released. The legacy WSI qualification remains isolated and does not define product presentation ownership.

Visible Vulkan content is owned by a native topmost DirectComposition target on one visible exact-size child HWND. Each retained backing is larger than the exact viewport and is completely refreshed with app-owned pixels. `IPresentationManager::Present` commits the selected full-capacity buffer with an identity transform; the child HWND clip is the exact-viewport owner.

Resize ordering accounts for screen-origin changes by matching the ANGLE window transaction. `WM_SIZING` only records the active edge and never exposes a proposed layout. Actual top-level `WM_SIZE` resizes the child to the exact client extent; the nested child `WM_SIZE` publishes metrics and waits through the matching Vulkan copy and Presentation submission terminal. The parent origin, child clip, and admitted raster are therefore ordered in one USER32 transaction for every edge. CompositionFrame, `DwmFlush`, and physical scan-out remain outside the bounded interactive wait. This uses no source translation, fixed FPS timer, or stretch.

The native/managed ABI remains GPU-pointer-free. Opaque Vulkan and Vulkan+Acrylic share the same native top raster target. Vulkan+Acrylic creates a non-topmost `Windows.UI.Composition.DesktopWindowTarget` on a dedicated system DispatcherQueue thread, calls `DesktopAcrylicController.SetTarget(WindowId, CompositionTarget)`, enables `DWMWA_USE_HOSTBACKDROPBRUSH`, and switches the native topmost Presentation surface to premultiplied alpha. The explicit mode requires the backdrop target, DesktopWindowTarget, active controller state, host-backdrop attribute, and premultiplied Vulkan surface; it neither degrades to plain transparency nor silently selects ANGLE. Kind, theme, tint, and luminosity options are applied to that controller with the shared last-wins option contract. No ContentIsland is created for the Vulkan topology.

SkiaSharp packages are pinned together at `4.152.0-rc.1.26426.14`. The Windows host directly references `SkiaSharp.Vulkan.Silk.NET` and constructs Skia's Vulkan backend through `GRSilkNetBackendContext` plus the typed `GRVkExtensions.Initialize` procedure callback instead of assembling the generic raw-handle context locally.

## Evidence and promotion boundary

The initial 2026-09-02 run passed package/loader capability checks, a short real-WSI qualification, actual `OUT_OF_DATE`/`SUBOPTIMAL`/`SURFACE_LOST`/`DEVICE_LOST` branch injection and recovery, exact resize/device-reset/minimize-restore/start-close ten-cycle gates, automated input/IME/UIA transport, default ANGLE regression, and self-contained empty-PATH launch on an NVIDIA GeForce RTX 4060 Laptop GPU. An `ANGLE → Vulkan → ANGLE` WGC sequence and all eight borders/corners at 600 px/600 ms reverse motion passed three consecutive runs without marker regressions, device/surface loss, or an outstanding acquired image. That validator did not yet enforce receipt-to-terminal cadence or target-to-present latency, so the result is not reused as smooth-resize acceptance.

After adopting acquire-as-presentation-commit, the AMD Radeon 780M on the same machine passed the maintenance-free capability and real-WSI contracts, product validation, and a Release DorotiDemoApp smoke run. The former exact-child/exact-swapchain design preserved one-to-one generations but repeatedly failed the strict same-run ANGLE latency gate: target-to-present p95 was 25.93 ms against a 20.31 ms allowance, and a hardened repeat reached 39.51 ms after a 96 ms swapchain-create outlier. Those failures remain historical evidence rather than being reclassified.

The 2026-09-03 retained oversized-child design removed swapchain recreation from normal in-capacity resize. A 10-cycle product run reported logical viewport 950x670 inside a 1280x752 surface, one surface creation, nine retained-surface reuses, ten accepted/presented generations, and zero out-of-date/device/surface-loss results. A source-stable `ANGLE → Vulkan → ANGLE` run of all eight borders/corners, three 600 px/600 ms reverse passes each, passed 24/24 automated cases with zero blank frames or Vulkan errors, one surface creation per process, 2,140 retained reuses, at least 119.15 presentations/s during motion, target-to-present p95 at most 4.95 ms, and maximum at most 9.00 ms. This is `PASS-automated-partial`, not physical scan-out or human-drag acceptance. The separate lifecycle qualification passed capability/WSI/product, actual result injection, reset/minimize/resize/start-close gates and source-to-binary correspondence. Hardened reruns use fresh output plus a repository-global single-writer lock and compare start/end HEAD, complete tracked diff, untracked files, key sources, product EXE+IL, probe EXE+IL, managed/native host, and observer identities as applicable.

Physical testing after that run reported transient black/white regions and delayed raster fitting, with visible internal jitter during left/top resize. That observation supersedes any inference of physical acceptance from the earlier automation. The proposed-size admission change addresses the remaining phase mismatch between an origin-moving top-level geometry transaction and the next logical-layout frame. Its final source-locked AMD strict run again passed all 24 edge/corner cases: 1,140/1,140 motion targets were presented with zero supersedes, platform-wait timeouts, or marker regressions, minimum presentation rate was 77.75 Hz, and worst target-to-present p95/maximum were 14.54/16.65 ms. The lower rate than the unconstrained retained-surface run reflects DWM admission pacing, not a hard FPS cap. Capability/real-WSI/product, five injected result branches, reset/minimize/resize/start-close repetitions also passed after making the lifecycle smoke advance only when USER32's actual iconic state matched the requested transition. This remains `PASS-automated-partial`; post-change human border resize and physical scan-out are `notVerified`.

A second physical check found the proposed-size change slightly better but still showed black/white regions. An intermediate revision moved its `DwmFlush` from the matching `WM_SIZE` into a successful `WM_SIZING` prepare, but the following physical check still failed and that provisional path has now been removed. Doroti deliberately does not paint an app-colored parent fallback: doing so would conceal rather than eliminate a top/child exposure. Iteration validation is intentionally focused: the default script now runs left and top-left once, `-Probe` runs top-left once, and the previous 24-case repetition requires `-FullEightEdgeMatrix`. A clean one-case AMD `TopLeft` probe of the actual-size blocking revision passed with 31 accepted motion targets, 32 presented motion terminals, zero platform-wait timeouts, and zero marker regressions. This remains automated partial evidence; current physical behavior is still `notVerified` until direct recheck.

The pre-geometry revision still failed direct physical inspection. Flutter's current Windows embedder instead marks resize started from the actual size, sends metrics, and blocks the platform thread for up to 100 ms while polling its task runner; the raster thread recreates/updates the exact surface and `OnFramePresented` marks the resize done before calling `DwmFlush`. Doroti now follows that actual-size blocking point while retaining its oversized Vulkan child to avoid AMD's per-step swapchain recreation. The former `WM_SIZING` prepare state and 16 ms wait are removed. No parent-color masking is present. The focused automated probe passes, but post-change physical live resize remains to be checked directly.

Timing diagnostics attributed the old AMD limit to `vkCreateSwapchainKHR`, normally about 19–25 ms for each exact extent. The retained child surface moves that cost to the hidden initial create and rare capacity growth instead of paying it for every viewport change. On the tested 1280x752 capacity the initial create took roughly 100–150 ms before first show; memory/startup cost on larger monitors and capacity growth across DPI/monitor changes remain matrix items, not assumed successes.

There are two independent specification boundaries. First, Khronos documents that unextended `vkQueueWaitIdle`/`vkDeviceWaitIdle` does not strictly prove that presentation resources are retired. This AMD device does not expose `VK_EXT_swapchain_maintenance1`, so the current practical queue-idle retirement remains `notVerified` at the specification level even though the automated lifecycle suite passes. Second, SkiaSharp does not expose Skia's Vulkan backend-layout query/update API. After fixing the acquire/layout barrier, an external synchronization-validation run positively proved loader insertion of `VK_LAYER_KHRONOS_validation` and emitted zero validation warning/error messages for the current validation-product workload; arbitrary-workload layout ownership remains unproven. These limits are additional reasons not to promote this presenter.

Slow/medium/fast × expand/shrink/reverse full Cartesian resize, per-edge ten-second stress, long WSI soak, human border resize, physical scan-out, the full GPU/driver/DPI/refresh matrix, physical Korean IME, and accessibility acceptance remain `notVerified` or `notRun`. Until those gates pass, Vulkan is experimental optional and must not become the default.

## 2026-09-03 direction-aware resize and Vulkan Acrylic amendment

A later physical report found that right/bottom resize felt good while left/top resize still made the inner raster appear to shake. The causal difference is the screen origin: the pre-geometry gate displayed the proposed logical layout while an origin-moving HWND was still at its old location, after which USER32 moved the complete window. The content therefore appeared to move once inside the old window and again with the committed window origin. The current direction split above removes that double movement while retaining the lower-latency gate where the origin is fixed.

A final source-built AMD Radeon 780M focused run passed both `Left` and `TopLeft` 600 ms reverse cases. They reported 27/25 outer changes, 26/24 presented motion terminals, 44.57/42.04 presentations/s, zero validation-background gap frames, exact final geometry, zero CompositionFrame waits/timeouts on the post-geometry path, no device loss, and stable source/repository/binary endpoints. A separate 24-case run proved the requested direction split and all transport/final-exact/resource contracts, but its aggregate status is intentionally `FAIL`: the trailing ANGLE reference capture failed one pixel-coverage frame, invalidating every relative cadence comparison, and 11 Vulkan cases reported at least one WGC pixel-coverage failure. This does not qualify the full matrix.

The superseded separate-target Vulkan+Acrylic revision passed its structural counters with no ContentIsland, three Presentation buffers, premultiplied alpha, 500/500 runtime option terminals with zero failures, clean shutdown, one Vulkan device reset, and minimize/restore. A headed DorotiDemoApp run also displayed material content, but the user's following check found plain transparency and remaining resize jitter. Those results are historical evidence only and do not qualify the current architecture.

## 2026-09-03 native top-target and SkiaSharp RC1 amendment

The direction-only revision above did not pass the user's next physical check, and its child-HWND Vulkan raster over a separate top-level Acrylic target produced plain transparency instead of convincing Acrylic. Those statements are retained as history, not current architecture. The current implementation is the top-level native DirectComposition raster target, atomic Presentation viewport transform, grow/shrink-ordered design in the ownership section.

Three consecutive focused AMD Radeon 780M runs of the native top-target revision passed `Left` and `TopLeft` reverse 600 ms cases with zero validation-background gaps, monotonic visible generation markers, final exact geometry, and clean resources. The targeted `TopRight` rerun also passed after the validation product added a right-edge background sentinel; this prevents valid foreground clipped at the minimum width from being mislabeled as a short composition surface. This is automated partial evidence, not physical smoothness acceptance.

The separate-target capability and capture above are historical because the user still observed plain transparency. Physical blur quality and a fresh human left/top drag remain `notVerified`.

## 2026-09-03 post-geometry and DWM-system-Acrylic amendment

The edge-translated native target revision also failed the user's next physical check. It has been replaced by a full-capacity identity Presentation source attached to the top-level HWND. Moving-origin edges defer their actual raster until default `WM_WINDOWPOSCHANGED` processing and a DWM geometry boundary have completed; the matching Presentation then observes its own CompositionFrame before its resize terminal is released.

Six consecutive source-built AMD Radeon 780M `TopLeft` reverse 600 ms runs passed with zero visible-generation regressions, validation-background gaps, or failed terminals. Separate `Left` and fixed-origin `Right` runs also passed. The current artifacts are under `.doroti/evidence/windows-vulkan-top-level-postgeometry-*-20260903`. These are automated capture/transport results, not a claim of physical smoothness.

Vulkan Acrylic now uses the top-level DWM transient-window system backdrop rather than a separate `DesktopAcrylicController` target. The current capability artifact at `.doroti/evidence/windows-vulkan-top-level-dwm-acrylic-capability-20260903/manifest.json` passes and records `DwmSystemBackdrop`, `TransientWindow`, redirection-bitmap alpha enabled, premultiplied Vulkan alpha, and three Presentation buffers. A current-monitor DorotiDemoApp frame at `.doroti/evidence/vulkan-dwm-acrylic-demo-20260903.monitor-frames/monitor-000160.png` visibly suppresses the sharp uncovered-wallpaper structure, while human material quality remains `notVerified`.

The repository package graph is pinned to `4.152.0-rc.1.26426.14`. The Windows presenter uses the same-version `SkiaSharp.Vulkan.Silk.NET` adapter and its `GRSilkNetBackendContext`; a force-evaluated restore of the full product solution succeeded across Windows, Web, Linux, Android, iOS, and macOS targets.

## 2026-09-04 top-level current/latest and Desktop Acrylic amendment

The synchronous post-geometry revision and its DWM transient-backdrop transport did not pass the user's next physical check. They remain historical evidence only. A subsequent parent-local retained-child trial proved the remaining ownership defect: even with a full-capacity identity source and synchronous child background paint, one `TopLeft` capture exposed 18 pixels of real wallpaper between the new top-level client and the previous child subtree. The product now attaches the native topmost DirectComposition target directly to the top-level HWND and keeps the native child hidden as capacity infrastructure. The HWND client and visible raster therefore have one geometry timeline.

Moving-origin interactive resize now keeps that current top-level raster attached while the worker coalesces to the latest actual `WM_SIZE` generation. It does not wait on each Vulkan copy or CompositionFrame; the final exit generation still settles synchronously. Three consecutive `TopLeft` reverse-600 ms runs passed with zero background gaps, marker regressions, timeouts, or device loss. The source-built run delivered 89 motion presents across 94 outer changes at 151.49 presentations/s with 7.65 ms accepted-to-next-present p95. Separate `Left` and `Right` regressions also passed; the fixed-origin `Right` case retained 53 successful CompositionFrame waits and zero timeouts.

Vulkan Acrylic now uses the documented `DesktopAcrylicController.SetTarget(WindowId, CompositionTarget)` Win32 path. A dedicated DispatcherQueue thread owns a non-topmost `DesktopWindowTarget`, the HWND has host-backdrop brush enabled, and the native topmost Vulkan target remains premultiplied above it. The final capability artifact at `.doroti/evidence/windows-vulkan-top-level-acrylic-capability-final-20260904/manifest.json` passed with controller state `Active`, all required target/host flags, three Presentation buffers, zero active WSI swapchains, and the complete reset/lifecycle/product package graph. A new Desktop Duplication capture at `.doroti/evidence/windows-vulkan-top-level-acrylic-visual-final-20260904` shows the sharp external wallpaper detail diffused beneath the Material surface. Physical drag smoothness, physical material quality, and the untested GPU/DPI/refresh matrix remain `notVerified`.

## 2026-09-04 exact-child ordering amendment

The top-level current/latest target above did not pass the user's next physical check: black/white exposure and left/top internal-raster jitter remained. Its automated results are retained as historical evidence but do not qualify physical behavior.

The current presenter returns the native child to a visible exact-size role and attaches the topmost DirectComposition Presentation target to that child. The top-level `WM_SIZE` calls `SetWindowPos` for the exact client extent, and the nested child `WM_SIZE` publishes actual metrics and waits through the matching Vulkan-copy and Presentation-submission terminal before the parent transaction returns. No proposed raster is exposed from `WM_SIZING`, and no interactive CompositionFrame or `DwmFlush` wait is used. Top-level input ingress and the top-level Desktop Acrylic target remain unchanged.

The source-built `TopLeft` reverse-600 ms artifact at `.doroti/evidence/codex-vulkan-exact-child-topleft-r2-20260904/manifest.json` passed with 69 motion presents across 70 outer changes, 118.29 presentations/s, 4.41 ms accepted-to-next-present p95, zero validation-background right gaps, and zero resize/CompositionFrame timeouts. `.doroti/evidence/codex-vulkan-exact-child-acrylic-20260904/manifest.json` passed the capability, exact-resize, reset, minimize/restore, start/close, and Acrylic-controller contracts. These remain automated partial evidence; the new binary's physical resize and Acrylic quality are `notVerified` pending direct user confirmation.

## 2026-09-04 Skia-first geometry admission and material-underlay amendment

The exact-child revision above also failed direct inspection: Acrylic rendered, but transient white/black regions and internal-raster jitter remained during resize, especially on left and top edges. The user explicitly chose complete pixels over maximum resize FPS. This amendment supersedes the ownership and resize-ordering paragraphs above; older sections remain failure history.

The native topmost DirectComposition Presentation target is attached directly to the top-level HWND, whose client is the single visible clip and geometry owner. The child HWND is hidden and carries retained monitor/DPI capacity only. `WS_EX_NOREDIRECTIONBITMAP` removes the independent USER32 white backing plane. Every interactive `WM_SIZING` computes the proposed client extent, grows retained capacity if needed, publishes that viewport, and waits for the exact Skia generation to complete Vulkan copy, Windows Presentation, and a DWM boundary before USER32 may adopt the proposed top-level geometry. Actual `WM_SIZE` normally observes identical metrics and emits no second raster; programmatic or mismatched changes retain a bounded exact-Present fallback. The 100 ms bound is a fail-safe, not a fixed cadence limiter.

Vulkan+Acrylic retains the documented host-backdrop-enabled non-topmost `DesktopWindowTarget` and active `DesktopAcrylicController`. It additionally enables an HWND-wide `DWMSBT_TRANSIENTWINDOW` underlay beneath the premultiplied topmost Vulkan target. The controller remains the normal Acrylic owner; the underlay supplies the same class of system material when a transient target-lag pixel would otherwise reveal white or black. Attachment fails explicitly if the controller, host backdrop, system underlay, or premultiplied target contract cannot be established.

The source-built opaque `TopLeft` artifact at `.doroti/evidence/codex-vulkan-skia-first-opaque-topleft-20260904/manifest.json` passed with zero validation-background gap frames or pixels, 49 motion presents, 53/53 observed Composition waits, zero timeouts, final exact geometry, and clean resources. The final Acrylic artifact at `.doroti/evidence/codex-vulkan-skia-first-underlay-acrylic-topleft-r3-20260904/manifest.json` passed transport, marker, final-exact, resource, top-level raster-owner, and DWM-wait contracts at 48 motion presents and 82.29 Hz, while the existing strict app-background coverage gate remained `FAIL` for 26 of 74 frames with a maximum 35-pixel material tail. Its manifest explicitly classifies this as `FAIL-strict-app-raster-coverage-material-underlay-may-be-visible`; inspected anomaly frames do not show a solid white/black bar, but that observation is neither strict app-raster coverage nor physical scan-out proof. The final source-built aggregate capability artifact at `.doroti/evidence/codex-vulkan-skia-first-underlay-capability-r2-20260904` passed. Physical left/top-left resize and Acrylic quality remain `notVerified` pending direct user confirmation.

## 2026-09-04 moving-origin post-geometry amendment

Direct inspection of the universal Skia-first candidate found no blank region and judged right/bottom resize nearly perfect, but left/top still showed ghost-like internal-raster jitter. The asymmetry identifies a second ordering issue: a proposed logical layout displayed at the old screen origin moves once inside that HWND and again when USER32 commits a left/top origin change.

The current policy is direction-aware on the same single top-level raster owner. `Right`, `Bottom`, and `BottomRight` retain proposed-size Skia Presentation and DWM admission before geometry. `Left`, `Top`, `TopLeft`, `TopRight`, and `BottomLeft` prepare retained capacity only; USER32 moves/clips the current identity raster with the HWND, then actual `WM_SIZE` publishes the exact metrics and waits for matching Skia/Vulkan Presentation plus a DWM boundary. The opaque retained guard or Acrylic transient-material underlay covers the post-geometry tail without a solid fallback color. No fixed FPS limiter, source translation, stretch, or visible child correction is introduced.

The presenter reports fail-closed direction counters. The source-built opaque `TopLeft` artifact at `.doroti/evidence/codex-vulkan-directional-postgeometry-opaque-topleft-20260904/manifest.json` is `PASS-automated-partial`: strict app-owned coverage reports zero gap frames and pixels, fixed-pre/moving-post/moving-pre-violation is `0/54/0`, all 54 DWM waits were observed with zero timeouts, final/resource/provenance gates pass, and motion ran at 83.41 Hz. The Acrylic `TopLeft` artifact at `.doroti/evidence/codex-vulkan-directional-postgeometry-acrylic-topleft-r2-20260904/manifest.json` records `0/49/0`, 49/49 DWM waits observed, zero timeouts, 75.43 Hz, and passing transport, marker, final-exact, resource, and owner gates. The `Right` artifact at `.doroti/evidence/codex-vulkan-directional-pregeometry-acrylic-right-r2-20260904/manifest.json` records `53/0/0`, 53/53 waits, zero timeouts, 82.32 Hz, and the same structural passes. Acrylic overall statuses remain `FAIL` under the strict opaque app-background sentinel oracle when material occupies a transient tail; inspected `TopLeft` anomaly frames show no solid white/black bar, but physical ghosting and scan-out remain `notVerified` pending another direct check. The final source-built aggregate at `.doroti/evidence/codex-vulkan-directional-acrylic-capability-r2-20260904` passes the v7 direction policy, Acrylic target/underlay, lifecycle, and source/repository/binary stability contracts. Its first attempt is retained as an ANGLE baseline failure after one stale-input copy was superseded (`Presents 24 != GpuCopies 25`); the successful repeat does not erase that intermittent evidence.

## 2026-09-04 moving-origin prepared-commit amendment

The user's next `TopLeft` screenshot resolved the remaining ambiguity. The new top-level client was already wider while the Skia grid still ended at the prior width; the exposed strip was Acrylic material from the underlay. Keeping the current raster through geometry therefore removed solid black/white exposure but introduced a real geometry/raster mismatch rather than fully solving the perceived ghosting.

Moving-origin resize now separates raster completion from visible admission. `WM_SIZING` publishes the proposed metrics, completes the Skia raster and synchronous Vulkan copy into an available Presentation slot, reserves that slot, and returns a distinct non-visible `Prepared` receipt. It does not complete the resize generation or expose the proposed layout at the old screen origin. After USER32 commits the top-level origin and extent, the actual `WM_SIZE` asks the raster worker to revalidate generation, input sequence, and extent, then presents the same reserved slot and waits for its CompositionFrame/DWM boundary. A mismatch cancels the preparation and performs an exact fallback render. Reserved slots are excluded from normal availability selection.

Fixed-origin right/bottom ordering is unchanged. No source transform, stretch, fixed resize FPS, or second visible HWND is introduced. The capability contract is v8 with policy `fixed-origin-pregeometry-present-moving-origin-pregeometry-prepare-postgeometry-present-dwm`.

The final-fingerprint opaque `TopLeft` artifact at `.doroti/evidence/codex-vulkan-prepared-commit-opaque-topleft-final-r2-20260904/manifest.json` is `PASS-automated-partial`: 53 preparations matched 53 post-geometry commits, 54/54 DWM waits were observed, no timeout or violation occurred, and strict app-owned coverage reported zero gap frames and pixels. Its immediately preceding run at `.doroti/evidence/codex-vulkan-prepared-commit-opaque-topleft-final-20260904/manifest.json` failed one of 67 captured frames with an 8-pixel strict app-raster gap despite passing ordering, DWM, final, and resource contracts; the repeat PASS does not erase that intermittent evidence. The final Acrylic `TopLeft` artifact at `.doroti/evidence/codex-vulkan-prepared-commit-acrylic-topleft-r2-20260904/manifest.json` records 51/51 prepare/commit, 52/52 observed waits, zero timeout/failure, exact final geometry, and passing transport/marker/resource/owner gates. Its aggregate remains `FAIL` because the strict opaque sentinel oracle reports material-tail pixels in 26 of 81 frames, maximum 27 pixels. The fixed-origin `Right` regression at `.doroti/evidence/codex-vulkan-prepared-commit-acrylic-right-20260904/manifest.json` retains 53 direct pre-geometry admissions, zero prepared receipts, 53/53 observed waits, and passing transport/final/resource gates. The source-built aggregate at `.doroti/evidence/codex-vulkan-prepared-commit-acrylic-capability-20260904/manifest.json` passes the v8 policy, Acrylic target/underlay, lifecycle, and source/repository/binary stability contracts. Inspected anomaly frames show the dark-blue app raster and test shapes reaching the client edge, but automated capture and Composition receipts still do not prove physical scan-out or human-perceived smoothness; a fresh direct `Left`/`TopLeft` check remains required.

## 2026-09-04 moving-origin pre-submit/no-display-wait amendment

The two-phase prepared/post-geometry candidate above also failed its next direct physical check. Left/top resize still appeared to shake, and the supplied capture again showed the top-level client wider than the currently visible Skia extent. That result supersedes the candidate's automated passes for physical acceptance.

The current direction split is at the display-admission boundary rather than the raster boundary. Every interactive edge publishes the proposed metrics, completes exact Skia rasterization and the synchronous Vulkan copy, and calls `IPresentationManager::Present` before USER32 applies geometry. Fixed-origin right/bottom edges continue to wait for a pre-geometry CompositionFrame/DWM boundary. Moving-origin left/top edges return after successful Present submission without forcing that new layout to scan out at the old screen origin; the immediately following USER32 geometry can therefore be consumed in the same compositor cycle. Matching actual `WM_SIZE` does not create a second viewport revision or Present. Programmatic, DPI, and mismatched resizes retain a bounded post-geometry exact fallback. `WM_EXITSIZEMOVE` observes one final DWM boundary at the actual geometry; it is not a per-frame resize wait.

The temporary `Prepared` terminal and reserved-slot path are removed. Capability contract v9 names the policy `fixed-origin-pregeometry-present-dwm-moving-origin-pregeometry-present-submit`. The final Acrylic `TopLeft` artifact at `.doroti/evidence/codex-vulkan-moving-submit-final-acrylic-topleft-20260904/manifest.json` records 97 moving-origin pre-geometry submissions, zero display waits at that boundary, zero timeout, exact final geometry, and passing transport/marker/resource/owner gates. Its overall status remains `FAIL`: the strict app-background oracle detected material-tail pixels in 23 of 82 frames, maximum 9 pixels. The final opaque `TopLeft` artifact at `.doroti/evidence/codex-vulkan-moving-submit-final-opaque-topleft-20260904/manifest.json` also remains `FAIL`: final/resource and 106/0 moving submissions/display waits pass, but two of 67 captured frames contain an exact app-raster gap of at most 18 pixels; inspected anomaly PNGs show the corresponding narrow right tail. An earlier zero-gap opaque pass does not erase this intermittent failure. The fixed-origin Acrylic `Right` regression preserves 53 pre-geometry admissions and 55/55 DWM waits with zero timeout, but remains overall `FAIL` for 5 of 69 strict sentinel frames, maximum 27 pixels. The current source-built aggregate at `.doroti/evidence/codex-vulkan-moving-submit-final-acrylic-capability-r4-20260904/manifest.json` passes v9, exact-LUID import, Acrylic, device-loss, exact-resize/reset/minimize/start-close, source-to-binary, and provenance stability contracts; its native SHA-256 is `1a00c5bc78b67c1c9c9b5b7118ade347d3e2661bba5f03d926505b9c488b62d0`. The first aggregate attempt remains recorded as an ANGLE baseline failure after one stale-input copy was superseded (`Presents 24 != GpuCopies 25`). Automated submission and WGC evidence do not establish physical scan-out or human smoothness; the new binary remains `repaired-pending-user-recheck / mixed automated evidence`.

## 2026-09-05 moving-origin 40 FPS pacing amendment

Direct physical testing of the pre-submit/no-display-wait candidate reported a small improvement but retained visible left/top shaking. The user explicitly accepted reducing live-resize cadence to 40 FPS. Moving-origin edges now pace the entire `WM_SIZING` geometry-plus-exact-raster admission transaction at 25 ms intervals before proposed metrics are published. This is intentionally not a renderer-only throttle: while waiting, the previous HWND geometry and previous exact Skia raster remain paired. After the deadline, the existing raster/copy/Present-before-geometry ordering runs unchanged. Fixed-origin right/bottom edges are not paced.

The native host uses a high-resolution waitable timer and records the actual wake time as the next pacing origin, preventing catch-up bursts after a slow raster. General-purpose sleeps were rejected after otherwise clean zero-gap runs measured only 33.53 and 30.22 FPS because of Windows timer-quantum overshoot. Capability contract v10 names `fixed-origin-pregeometry-present-dwm-moving-origin-40fps-pregeometry-present-submit` and declares `movingOriginMaximumFps=40`.

The high-resolution opaque `TopLeft` artifact at `.doroti/evidence/codex-vulkan-moving-40fps-hires-opaque-topleft-20260905/manifest.json` is `PASS-automated-partial`: 87 captures contain zero strict gap pixels, motion runs at 37.99 presentations/s, moving submissions/display waits are 28/0, and final/resource gates pass. The Acrylic companion at `.doroti/evidence/codex-vulkan-moving-40fps-hires-acrylic-topleft-20260905/manifest.json` runs at 39.43 presentations/s and passes transport, marker, final, resource, and 28/0 ordering, but remains overall `FAIL` because the strict oracle detects material tail in 31 of 73 captures, maximum 59 pixels. Automated capture cannot decide whether the lower physical cadence feels steadier or merely choppier, so direct left/top/top-left acceptance remains `notVerified`.

The current source-built aggregate at `.doroti/evidence/codex-vulkan-moving-40fps-hires-capability-r2-20260905/manifest.json` passes the v10/40 FPS contract, exact-LUID import, Acrylic, device-loss, exact resize/reset/minimize/start-close, source-to-binary, and source/repository/binary stability gates. Its native SHA-256 is `8b787b3375c16a2715e1891f343d649cedae616556e9fd7ab7bd9e873d73eb60`. The first v10 aggregate remains recorded as the known intermittent ANGLE baseline stale-input-copy failure (`Presents 24 != GpuCopies 25`); the successful repeat does not erase it.

## 2026-09-05 physical rejection and ownership boundary amendment

The user's direct Vulkan+AMD+Acrylic retest still showed left/top shaking at 40 FPS. That result rejects the pacing candidate for physical acceptance despite its automated transport, ordering, final-geometry, and zero-gap opaque evidence. The high-resolution pacing state and the v10-only maximum-FPS contract have been removed; the source returns to the unpaced v9 direction-specific submission policy. The v10 artifacts remain historical evidence and are not reclassified.

This closes cadence as the repair axis. With a standard top-level HWND, USER32/DWM owns the moving origin and extent while the Presentation frame and clip are committed through DirectComposition. The two transactions may land in one compositor cycle but cannot be committed atomically by the application. Fixed-origin right/bottom resize hides this boundary; moving-origin left/top resize exposes it as a double movement.

An app-owned fixed envelope and custom non-client shell could move border, chrome, content, and exact Skia raster offset/clip/front selection into one application transaction. The user chose not to adopt that Arm N direction, so no related ABI or positioned-Presentation implementation is retained. There is currently no approved successor implementation; standard-HWND left/top physical acceptance remains unresolved. ANGLE remains the default, and Vulkan remains explicit opt-in with no silent fallback.

## 2026-09-05 clock, actual geometry, and matching receipt candidate

Following the user's request for a new repair, the active candidate now prepares an exact moving-origin frame on the raster worker without binding its slot to the visible surface. `WM_WINDOWPOSCHANGING` validates the final proposed key after default min/max handling and waits once for the compositor clock, bounded at 32 ms. USER32 applies geometry. At `WM_WINDOWPOSCHANGED` entry, the platform thread revalidates actual geometry, immediately submits the prepared slot, and waits up to 50 ms for a CompositionFrame statistic with the same present ID/content tag and a nonempty display-instance array. Native wait mode 2 does not substitute `DwmFlush` for that receipt. Matching `WM_SIZE` acknowledges the commit without another raster. Fixed-origin ordering and Acrylic topology remain unchanged.

The required native `PREPARED_GEOMETRY_RECEIPT_V1` feature prevents a new managed host from silently running against an older DLL. Reserved slots cannot enter ordinary acquisition; cancellation, stale input, mismatch, reset, loss, and shutdown drain their reservation and terminal. Clock timeout continues submission but records a qualification failure. The baseline capability contract remains v9 pending physical acceptance; the actual product policy is `moving-origin-clock-geometry-prepared-commit-receipt`, recorded separately from the baseline in aggregate evidence.

Pre-geometry clock/Present still exposed 17 pixels during slow Acrylic Left resize. Post-geometry submission without a matching receipt intermittently exposed 9 pixels during TopLeft resize; removing pre-clock alignment from the receipt variant exposed 19 pixels. Those candidates and failures are archived. The selected clock/geometry/receipt candidate passed 16 focused cases on AMD Radeon 780M at 96 DPI/165 Hz: Acrylic Left/Top/TopLeft at 150/600/1200 ms, five opaque TopLeft repeats, and opaque Right/Bottom. Its 1,389 captures had no above-threshold right-gap frame, and 540 prepared commits passed QPC ordering. Evidence is `.doroti/evidence/codex-postgeometry-receipt-checks-20260905/matrix-summary.json`. The source-built capability aggregate r2 passes ANGLE, Vulkan/Acrylic, device loss, and lifecycle gates; its first ANGLE stale-input-copy baseline failure remains recorded. An injected clock timeout produces exactly one failure, drains reservations, preserves final exactness, and correctly leaves qualification FAIL.

The fixed-right pixel oracle now uses an invariant screen-space right edge and exact app sentinel instead of inferring an Acrylic window boundary from caption colors. It requires usable evidence in every captured frame and invariant atomic outer-right/DPI samples. The existing pixel threshold is unchanged; synthetic tests and replay of untouched historical PNGs distinguish the caption false positives from the real 17/9/19-pixel failures. Other capture configurations retain the old caption oracle and its known Acrylic limitation. No historical manifest is rewritten.

This candidate has a material cadence cost: moving-origin throughput measured 52.50–59.58 presentations/s, despite a 165 Hz display. There is no fixed FPS target, but the clock and receipt waits serialize resize. The standard HWND and Presentation still cannot be committed atomically by the application. Physical jitter, smoothness, Acrylic quality, other GPU/DPI/refresh conditions, and the full edge/ANGLE cadence matrix remain unaccepted or unrun. The active candidate is an implemented repair for direct recheck, not a declaration that the prior physical failure has been resolved. The implementation and evidence from former `work.md` section 33 are preserved in the [September 5 summary](../../../history/26-09-05/windows-vulkan-acrylic-resize-summary.md).

## 2026-09-05 user resize acceptance and archival

The user subsequently confirmed that window resizing now works well and requested archival of `problem.md` and `work.md`. The clock/geometry/receipt implementation is now **PASS-observed for window resizing in the user's Vulkan+AMD+Acrylic environment**. This supersedes the pending resize verdict immediately above, while all earlier candidate failures remain historical evidence. Other GPU/DPI/refresh combinations and unperformed acceptance checks are not promoted. The separate initial-FPS observation, successful ReadyToRun publish/startup smoke, and unmeasured R2R performance effect are preserved in the September 5 summary. The root problem/work files were removed after that summary was created; runtime code and capability schema were not changed by this archival step.
